const ServiceError = require('../core/serviceError');

// const handleDBError = (error) => {
//     const { code = '', sqlMessage } = error;

//     if (code === 'ER_DUP_ENTRY') {
//         switch (true) {
//             case sqlMessage.includes()
//         }
//     }
// }

// module.exports = handleDBError;